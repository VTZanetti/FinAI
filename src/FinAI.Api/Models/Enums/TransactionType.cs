namespace FinAI.Api.Models.Enums;

/// <summary>
/// Tipo da transação — derivado do sinal do valor (positivo = Income, negativo = Expense).
/// </summary>
public enum TransactionType
{
    Income = 1,
    Expense = 2
}
